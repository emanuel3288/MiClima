// Eliminar importaciones relacionadas con el modo oscuro
// import { Switch, FormControlLabel } from '@mui/material';

// Eliminar el componente Header
// const Header = ({ isDarkMode, toggleTheme }: HeaderProps) => {
//   return (
//     <header style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 20px', backgroundColor: isDarkMode ? '#333' : '#f5f5f5' }}>
//       <h1>Weather App</h1>
//       <FormControlLabel
//         control={<Switch checked={isDarkMode} onChange={toggleTheme} />}
//         label={isDarkMode ? 'Modo Oscuro' : 'Modo Claro'}
//       />
//     </header>
//   );
// };

// export default Header; 